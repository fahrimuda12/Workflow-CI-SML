name: heart_disease_retrain

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: str
        default: "dataset/failure_heart_preprocessing.csv"
    command: "python modelling.py"
